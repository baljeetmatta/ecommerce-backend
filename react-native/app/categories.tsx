import { Ionicons } from "@expo/vector-icons";import { router } from "expo-router";import { FlatList,Pressable,StyleSheet,Text,View } from "react-native";import { Screen } from "@/components/Screen";import { useShop } from "@/context/ShopContext";import { colors } from "@/theme";
export default function Categories(){const{store}=useShop();return <Screen title="Categories" back><FlatList data={store.categories} contentContainerStyle={s.list} keyExtractor={c=>c._id} renderItem={({item,index})=><Pressable onPress={()=>router.push({pathname:"/(tabs)/shop",params:{category:item._id}})} style={s.item}><View style={[s.icon,{backgroundColor:["#e8f5ed","#fff2dc","#e8effb","#f7e9f2"][index%4]}]}><Ionicons name="leaf-outline" size={24} color={colors.primary}/></View><View style={{flex:1}}><Text style={s.name}>{item.name}</Text><Text style={s.sub}>Browse products</Text></View><Ionicons name="chevron-forward" size={20} color={colors.muted}/></Pressable>}/></Screen>};const s=StyleSheet.create({list:{padding:16,gap:11},item:{height:78,backgroundColor:"white",borderRadius:16,borderWidth:1,borderColor:colors.line,padding:12,flexDirection:"row",alignItems:"center",gap:13},icon:{width:52,height:52,borderRadius:15,alignItems:"center",justifyContent:"center"},name:{fontSize:16,fontWeight:"800",color:colors.ink},sub:{fontSize:12,color:colors.muted,marginTop:3}});
