import { router } from "expo-router";import { useState } from "react";import { KeyboardAvoidingView,Platform,ScrollView,StyleSheet,Text,Pressable,View } from "react-native";import { Screen } from "@/components/Screen";import { Button,Field } from "@/components/Ui";import { useAuth } from "@/context/AuthContext";import { colors } from "@/theme";
export default function Login(){const{login}=useAuth();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[busy,setBusy]=useState(false);const[error,setError]=useState("");const submit=async()=>{setBusy(true);setError("");try{await login(email.trim().toLowerCase(),password);router.replace("/(tabs)/account")}catch(e:any){setError(e.message)}finally{setBusy(false)}};return <Screen title="Sign in" back><KeyboardAvoidingView behavior={Platform.OS==="ios"?"padding":undefined} style={{flex:1}}><ScrollView contentContainerStyle={s.content} keyboardShouldPersistTaps="handled"><View style={s.icon}><Text style={{fontSize:34}}>🛍️</Text></View><Text style={s.title}>Welcome back</Text><Text style={s.subtitle}>Sign in to sync your cart, track orders and checkout faster.</Text>{error?<Text style={s.error}>{error}</Text>:null}<Field label="Email address" value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address" placeholder="you@example.com"/><Field label="Password" value={password} onChangeText={setPassword} secureTextEntry placeholder="Your password"/><Button title="Sign in" loading={busy} onPress={submit}/><Pressable onPress={()=>router.push("/signup")}><Text style={s.link}>New to HRS Basket? <Text style={{fontWeight:"900"}}>Create account</Text></Text></Pressable></ScrollView></KeyboardAvoidingView></Screen>};const s=StyleSheet.create({content:{padding:24,gap:16},icon:{width:72,height:72,borderRadius:22,backgroundColor:colors.primarySoft,alignItems:"center",justifyContent:"center",alignSelf:"center",marginTop:16},title:{fontSize:29,fontWeight:"900",textAlign:"center",color:colors.ink},subtitle:{color:colors.muted,textAlign:"center",lineHeight:21,marginBottom:10},error:{color:colors.danger,backgroundColor:"#fff0f0",padding:12,borderRadius:10},link:{color:colors.primary,textAlign:"center",marginTop:6}});

