import { router } from "expo-router";import { useState } from "react";import { KeyboardAvoidingView,Platform,ScrollView,StyleSheet,Text,View } from "react-native";import { Screen } from "@/components/Screen";import { Button,Chip,Field } from "@/components/Ui";import { useAuth } from "@/context/AuthContext";import { colors } from "@/theme";
export default function Signup(){const{register}=useAuth();const[f,setF]=useState({name:"",email:"",password:"",confirmPassword:"",gender:"prefer_not_to_say"});const[busy,setBusy]=useState(false);const[error,setError]=useState("");const set=(k:string,v:string)=>setF(x=>({...x,[k]:v}));const submit=async()=>{setBusy(true);setError("");try{await register(f);router.replace("/(tabs)/account")}catch(e:any){setError(e.message)}finally{setBusy(false)}};return <Screen title="Create account" back><KeyboardAvoidingView style={{flex:1}} behavior={Platform.OS==="ios"?"padding":undefined}><ScrollView contentContainerStyle={s.content} keyboardShouldPersistTaps="handled"><Text style={s.title}>Join HRS Basket</Text><Text style={s.sub}>One account for faster checkout, saved carts and order updates.</Text>{error&&<Text style={s.error}>{error}</Text>}<Field label="Full name" value={f.name} onChangeText={v=>set("name",v)} placeholder="Your name"/><Field label="Email address" value={f.email} onChangeText={v=>set("email",v)} autoCapitalize="none" keyboardType="email-address"/><Text style={s.label}>Gender</Text><ScrollView horizontal contentContainerStyle={{gap:8}}>{[["female","Female"],["male","Male"],["other","Other"],["prefer_not_to_say","Prefer not to say"]].map(([v,l])=><Chip key={v} label={l} selected={f.gender===v} onPress={()=>set("gender",v)}/>)}</ScrollView><Field label="Password" value={f.password} onChangeText={v=>set("password",v)} secureTextEntry placeholder="At least 8 characters"/><Field label="Confirm password" value={f.confirmPassword} onChangeText={v=>set("confirmPassword",v)} secureTextEntry/><Button title="Create my account" loading={busy} onPress={submit}/></ScrollView></KeyboardAvoidingView></Screen>};const s=StyleSheet.create({content:{padding:22,gap:15,paddingBottom:40},title:{fontSize:28,fontWeight:"900",color:colors.ink},sub:{color:colors.muted,lineHeight:21,marginBottom:6},error:{color:colors.danger,backgroundColor:"#fff0f0",padding:12,borderRadius:10},label:{fontSize:13,fontWeight:"700",color:colors.ink}});

